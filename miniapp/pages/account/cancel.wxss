@import "../../styles/commercialization.wxss";

.agree-row {
  display: flex;
  align-items: flex-start;
  gap: 16rpx;
}

.agree-text {
  flex: 1;
  font-size: 25rpx;
  line-height: 1.7;
  color: var(--text-secondary);
}
